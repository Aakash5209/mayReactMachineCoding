import React,{useState} from "react";


export const Login = ({name, address})=>{

    return(<>
    <h1>hi i m login page for - {name} who is in -{address} </h1>
    </>)
}

export default Login;
